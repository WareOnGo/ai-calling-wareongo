node_modules
.next
.env
.env.local
*.log
.DS_Store
.vercel
next-env.d.ts
*.tsbuildinfo

# Source datasets & cleaned outputs (large; not for version control)
rawdata/
cleandata/

# DB schema + data pipeline — kept private (sources, schemas, migrations, loaders)
scripts/
sql/
